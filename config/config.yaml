ENVIRONMENT: production
SERVER:
  ADDR: ":8080"
  READ_TIMEOUT: 5s
  WRITE_TIMEOUT: 10s
  IDLE_TIMEOUT: 120s
DATABASE:
  PATH: "data/dvarapala.db"
LOG:
  DIR: "log"
AUTH:
  JWT_SECRET: "change-me-in-production"
  JWT_EXPIRY: 24h
